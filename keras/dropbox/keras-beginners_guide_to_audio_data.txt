# keras-beginners_guide_to_audio_data.txt

# Source: Beginner's Guide to Audio Data, https://www.kaggle.com/fizzbuzz/beginner-s-guide-to-audio-data/data
# Link to download data: FSDKaggle2018 (for Freesound General-Purpose Audio Tagging Challenge), https://zenodo.org/record/2552860#.XL2JcN9fhuV

# Ref. Document: Mel Frequency Cepstral Coefficient (MFCC) tutorial, http://practicalcryptography.com/miscellaneous/machine-learning/guide-mel-frequency-cepstral-coefficients-mfccs/

import numpy as np
import os
import shutil

import IPython
import matplotlib
import matplotlib.pyplot as plt
import pandas as pd
import seaborn as sns

from tqdm import tqdm_notebook
from sklearn.cross_validation import StratifiedKFold

import librosa
import scipy
from keras import losses, models, optimizers

np.random.seed(1001)
matplotlib.style.use('ggplot')

# /home/user/audio_classification/FSDKaggle2018
# $ docker cp FSDKaggle2018.zip optimistic_varahamihira:/home/user/audio_classification/FSDKaggle2018
# ERRO[0001] Can't add file /home/aimldl/Downloads/kaggle/FSDKaggle2018/FSDKaggle2018.zip to tar: archive/tar: write too long
# $

fname,label,manually_verified,freesound_id,license
00044347.wav,Hi-hat,0,28739,Attribution
...
fff81f55.wav,Cough,1,19117,Attribution


>>> import pandas as pd
>>> train = pd.read_csv('train_post_competition.csv')
>>> train
             fname                  label         ...          freesound_id             license
0     00044347.wav                 Hi-hat         ...                 28739         Attribution
...
9472  fff81f55.wav                  Cough         ...                 19117         Attribution

[9473 rows x 5 columns]

>>> train.shape
(9473, 5)
>>> train.shape[0]
9473
>>> train.shape[1]
5

>>> import pandas as pd
>>> train = pd.read_csv('train_post_competition.csv')
>>> train
             fname                  label         ...          freesound_id             license
0     00044347.wav                 Hi-hat         ...                 28739         Attribution

category_group = train.groupby(['label', 'manually_verified']).count()
plot = category_group.unstack().reindex(category_group.unstack().sum(axis=1).sort_values().index)\
          .plot(kind='bar', stacked=True, title="Number of Audio Samples per Category", figsize=(16,10))

    self.tk = _tkinter.create(screenName, baseName, className, interactive, wantobjects, useTk, sync, use)
_tkinter.TclError: no display name and no $DISPLAY environment variable

